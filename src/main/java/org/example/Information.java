package org.example;

public class Information {

    public static Information getInformation(String informationCode){

        Information i = null
                Details d = new Details();
        if (informationCode.equalsIgnoreCase(anotherString: "java"))
        {
            i.SetCode(productCode);
            i.setStamp("Travel App");
            i.setVerify();
            i.setAuthentication("");
            i = d;
        }
        else if (informationCode.equalsIgnoreCase(another.String:destination"))
        {
          Event e = new Event();
          e.setCode("");
          e.setDescription("");
          e.setPrice();
          e.setLink();
          i =e;
        }
        else if (informationCode.equalsIgnoreCase(anotherString: ""))
        {
            Music m = new Music();
            m.setCode("");
            m.setDescription("");
            m.setList();
            m.setArtist();
        }
    }
}
